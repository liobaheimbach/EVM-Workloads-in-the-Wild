# Copy this file to .env and fill in your values
# RAW_BASE_DIR: directory for raw output (CSV files, parquet)
RAW_BASE_DIR=/path/to/raw_data

# DB_BASE_DIR: directory for processed databases (DuckDB, parquet)
DB_BASE_DIR=/path/to/db_data
