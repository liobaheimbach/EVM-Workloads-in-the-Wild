# Legacy helpers

`label_create_opcode_transactions.py` implements an older definition that
classified every transaction invoking CREATE or CREATE2 as contract creation.
It is retained for provenance and is not called by the current workflows.
